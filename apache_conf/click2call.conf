# Apache reverse proxy configuration for Click2Call Python API
<Location /api/v1>
    <IfModule mod_proxy.c>
        ProxyPreserveHost On
        ProxyPass http://127.0.0.1:8000/api/v1
        ProxyPassReverse http://127.0.0.1:8000/api/v1
    </IfModule>

    # Add security headers
    Header set X-Frame-Options "DENY"
    Header set X-Content-Type-Options "nosniff"
    Header set X-XSS-Protection "1; mode=block"
    Header set Access-Control-Allow-Origin "*"
    Header set Access-Control-Allow-Methods "POST, GET, OPTIONS"
    Header set Access-Control-Allow-Headers "Content-Type"
</Location>

# Logging
ErrorLog ${APACHE_LOG_DIR}/click2call_error.log
CustomLog ${APACHE_LOG_DIR}/click2call_access.log combined